Assets (images, icons) used by the GUI.

- model-icons/  — Icons for Recommended Models cards (see model-icons/README.txt).
- GitHub.png    — Optional: 256×80 px image for the footer "GitHub" button (Octocat + text).
                  If present in this folder (App/assets/GitHub.png), the footer shows it
                  scaled to 96×30; otherwise the footer shows the text "GitHub" only.
